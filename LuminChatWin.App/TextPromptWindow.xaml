<Window x:Class="LuminChatWin.App.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入文本"
        Width="420"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptTitleTextBlock" FontSize="22" FontWeight="SemiBold"/>
        <TextBlock x:Name="PromptTextBlock" Grid.Row="1" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"/>
        <TextBox x:Name="ResponseTextBox" Grid.Row="2" Margin="0,16,0,0" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="取消" Style="{StaticResource SubtleButtonStyle}" Click="Cancel_Click"/>
            <Button Content="确定" Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>